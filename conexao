
import mysql.connector

def connect():
    mybd = mysql.connector.connect(
        host="localhost",
        user="root",
        password="",
        database="sistema_biblioteca"
    )
    return mybd
